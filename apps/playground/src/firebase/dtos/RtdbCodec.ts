/**
 * Firebase Realtime Database Key Codec.
 * Re-exports from @jantt/core to maintain a single source of truth across the monorepo.
 */

export {
  encodeRtdbKey,
  decodeRtdbKey,
  encodePlanForRtdb,
  decodePlanFromRtdb
} from "@jantt/core";
